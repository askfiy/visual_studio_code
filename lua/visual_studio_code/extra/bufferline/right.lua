---@diagnostic disable-next-line: unused-local
return function(conf, colors)
    return function()
        return {
            { text = "  ", bg = colors.__vscode_local_background, fg = colors.__vscode_onactive_table_foreground },
            { text = "  ", bg = colors.__vscode_local_background, fg = colors.__vscode_onactive_table_foreground },
            { text = "  ", bg = colors.__vscode_local_background, fg = colors.__vscode_onactive_table_foreground },
        }
    end
end
